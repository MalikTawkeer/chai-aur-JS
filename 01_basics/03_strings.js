const name = 'malik'
const repoCount = 12

// console.log(name + repoCount + 'value')  // Not a Modern approach

//modern approach STRING INTERPOLATION uses bactics
console.log(`my name is ${name.toUpperCase()} and repocount is ${repoCount}`)

